import {VNDCurrencyPipe} from './vndcurrency.pipe';

describe('VNDCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new VNDCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
